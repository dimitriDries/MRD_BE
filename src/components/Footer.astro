---
import { site, nav } from "../data/site";
const year = new Date().getFullYear();
---

<footer class="footer" data-section="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <img src="/images/mrd_thumbnail.png" alt="" width="44" height="44" loading="lazy" />
      <p class="footer__name">Monsieur<strong>Data</strong></p>
      <p class="footer__tag">{site.owner.role}, {site.owner.location}</p>
    </div>

    <nav class="footer__nav" aria-label="Footer navigation">
      <p class="footer__heading">Site</p>
      <ul>
        {nav.links.map((link) => (
          <li><a href={link.href} data-cta data-cta-id={link.href.split("#").pop()} data-cta-type="link" data-cta-category="navigation">{link.label}</a></li>
        ))}
        <li><a href="/#contact" data-cta data-cta-id="contact" data-cta-type="link" data-cta-category="navigation">Contact</a></li>
      </ul>
    </nav>

    <div class="footer__contact">
      <p class="footer__heading">Get in touch</p>
      <ul>
        <li><a href={`mailto:${site.email}`} data-cta data-cta-id="email">{site.email}</a></li>
        <li><a href={site.linkedin} rel="noopener" target="_blank" data-cta data-cta-id="linkedin">LinkedIn</a></li>
      </ul>
    </div>
  </div>

  <div class="container footer__legal">
    <p>&copy; {year} {site.name} &middot; {site.owner.name}</p>
    <nav class="footer__legal-links" aria-label="Legal">
      <a href="/privacy" data-cta data-cta-id="privacy" data-cta-type="link">Privacy Policy</a>
      <a href="/cookies" data-cta data-cta-id="cookies" data-cta-type="link">Cookie Policy</a>
      <a href="/legal" data-cta data-cta-id="legal" data-cta-type="link">Legal Notice</a>
      <button type="button" class="footer__cookie-btn" data-cookie-preferences>Cookie preferences</button>
    </nav>
    <p>BTW BE 1007.857.714</p>
  </div>
</footer>

<style>
  .footer {
    background: var(--ink);
    color: var(--paper-inv);
    padding-block: 4rem 2rem;
    /* the slash, as a top border */
    border-top: 4px solid var(--red);
  }

  .footer__inner {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 3rem;
    padding-bottom: 3rem;
  }

  .footer__name {
    font-family: var(--font-display);
    font-size: 1.3rem;
    margin-top: 0.9rem;
  }

  .footer__name strong { color: var(--red); }

  .footer__tag {
    color: var(--muted-inv);
    font-size: 0.95rem;
    margin-top: 0.3rem;
  }

  .footer__heading {
    font-family: var(--font-mono);
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--muted-inv);
    margin-bottom: 1rem;
  }

  .footer__heading::before {
    content: "/ ";
    color: var(--red);
  }

  .footer ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.6rem;
  }

  .footer a {
    color: var(--paper-inv);
    text-decoration: none;
    opacity: 0.85;
    transition: opacity 0.2s, color 0.2s;
  }

  .footer a:hover {
    opacity: 1;
    color: #fff;
  }

  .footer__legal {
    border-top: 1px solid color-mix(in srgb, var(--paper-inv) 14%, transparent);
    padding-top: 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem 1.5rem;
    color: var(--muted-inv);
    font-size: 0.875rem;
  }

  .footer__legal-links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem 1.25rem;
  }

  .footer__cookie-btn {
    background: none;
    border: 0;
    padding: 0;
    font: inherit;
    color: var(--muted-inv);
    cursor: pointer;
    opacity: 0.85;
    transition: opacity 0.2s, color 0.2s;
  }

  .footer__cookie-btn:hover {
    opacity: 1;
    color: #fff;
  }

  @media (max-width: 700px) {
    .footer__inner {
      grid-template-columns: 1fr;
      gap: 2.25rem;
    }
  }
</style>
